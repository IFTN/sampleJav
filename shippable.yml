# Language setting
language: java

# Version number
jdk:
  - openjdk6
  - openjdk7
  - oraclejdk7
  #- oraclejdk8
matrix:
  allow_failures:
    - jdk: oraclejdk7 
build:
  ci:
    - java -version  
    - echo $custom
    - if [ "$SHIPPABLE_JDK_VERSION" == "oraclejdk7" ]; then foobar; fi
    - for i in {1..5}; do echo "$i"; done
    #- echo $custom
#    - sleep 8m 
   
